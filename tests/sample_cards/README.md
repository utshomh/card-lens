# Sample cards

Place `.jpg`, `.jpeg`, `.png`, or `.bmp` business-card images in this directory.
`pytest -s tests/test_kie.py` scans every image and prints the extracted fields.
No KIE model path or label-file configuration is required.
